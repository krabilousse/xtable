<%= form_for(@group_invitation) do |f| %>
  <% if @group_invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_invitation.errors.count, "error") %> prohibited this group_invitation from being saved:</h2>

      <ul>
      <% @group_invitation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
