<%= bootstrap_form_for(@emptySearch, url:{action: "search"}, method: "get", layout: :inline) do |f| %>
  <%= f.search_field :name, {hide_label: :true} %>
  <%= f.submit "search", {class: "btn btn-primary"} %>
<% end %>